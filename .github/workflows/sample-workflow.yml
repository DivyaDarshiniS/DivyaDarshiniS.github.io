name: Email-In-Step Workflow

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 24ds3000004@ds.study.iitm.ac.in
        run: echo "This step includes an email address in its name"